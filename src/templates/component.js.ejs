import React from 'react'

import { Container } from './styles'

export default function <%= props.name %>(<%= props.componentParameters %>) {
  <% if(props.options.s){ %>
    const {} = state
  <% } %>

  <% if(props.options.a){ %>
    const {} = actions
  <% } %>

  return <Container><%= props.name %></Container>
}
